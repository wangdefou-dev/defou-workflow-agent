# 本地文章重写技能

## 描述
该技能监听 `input` 目录下的 Markdown (`.md`) 或文本 (`.txt`) 文件。当检测到文件时，它会将内容重写为两种风格：
1. **Defou x Stanley**：深度认知洞察 + 病毒式结构。
2. **睿智幽默**：一针见血、冷幽默且反常识。

## 使用方法
1. 运行 `npm run skill:local`。
2. 将你的文章文件放入 `input/` 目录。
3. 在 `outputs/rewritten_articles/` 目录查看结果。
